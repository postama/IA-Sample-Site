---
/* ProblemStatement.astro */
---

<section class="problem-wrapper" id="problem-trigger">
    <div class="noise-overlay"></div>
  
    <div class="content-container">
      
      <div class="stat-block fade-item">
        <div class="hazard-strip"></div>
        <div class="stat-content">
          <div class="meta-row">
              <span class="tech-label">ERR_LOG: REVENUE_LEAKAGE</span>
              <span class="tech-label">PRIORITY: CRITICAL</span>
          </div>
          <h2 class="stat-headline">
            Supply chain exceptions cost brands <span class="highlight">13-28%</span> of annual revenue.
          </h2>
        </div>
      </div>
  
      <div class="issues-grid">
        
        <div class="issue-card fade-item">
          <div class="card-header">
            <span class="tech-label">REF: TRD-01</span>
            <svg class="status-icon" viewBox="0 0 24 24"><path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z"></path><line x1="12" y1="9" x2="12" y2="13"></line><line x1="12" y1="17" x2="12.01" y2="17"></line></svg>
          </div>
          <h3>Trade & Geopolitical Volatility</h3>
          <p>Constant tariff changes and overnight policy shifts render forecasts obsolete.</p>
        </div>
  
        <div class="issue-card fade-item">
          <div class="card-header">
            <span class="tech-label">REF: OPS-02</span>
            <svg class="status-icon" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10"></circle><line x1="12" y1="8" x2="12" y2="12"></line><line x1="12" y1="16" x2="12.01" y2="16"></line></svg>
          </div>
          <h3>Operational Disruptions</h3>
          <p>Delays, ETA slips, inventory shortages, and forced supplier substitutions.</p>
        </div>
  
        <div class="issue-card fade-item">
          <div class="card-header">
            <span class="tech-label">REF: DSG-03</span>
            <svg class="status-icon" viewBox="0 0 24 24"><polyline points="4 7 4 4 20 4 20 7"></polyline><line x1="9" y1="20" x2="15" y2="20"></line><line x1="12" y1="4" x2="12" y2="20"></line></svg>
          </div>
          <h3>Design & Part Changes</h3>
          <p>Component changes ripple through inventory and orders, breaking the chain.</p>
        </div>
  
      </div>
  
      <div class="chaos-footer fade-item">
        <div class="footer-label">
          <span class="tech-label">SYSTEM DIAGNOSIS:</span>
        </div>
        <div class="footer-content">
          <h3>CONSTANT CHAOS</h3>
          <p>Changes are logged, but not prioritized. Teams scramble offline to assess impact.</p>
        </div>
        <div class="footer-status">
          <span class="tech-label blinking">ATTN REQUIRED</span>
        </div>
      </div>
  
    </div>
  </section>
  
  <style>
    /* --- SCOPED VARIABLES --- */
    .problem-wrapper {
      --bg-manila: #E3A857;
      --ink-black: #111111;
      --grid-line: 1px solid var(--ink-black);
      --font-main: 'Archivo', sans-serif;
      --font-tech: 'JetBrains Mono', monospace;
  
      position: relative;
      background-color: var(--bg-manila);
      color: var(--ink-black);
      font-family: var(--font-main);
      width: 100%;
      padding: 6rem 1rem;
      overflow: hidden;
    }
  
    .problem-wrapper * { box-sizing: border-box; }
  
    /* --- TEXTURE --- */
    .noise-overlay {
      position: absolute;
      top: 0; left: 0; width: 100%; height: 100%;
      pointer-events: none; z-index: 1; opacity: 0.12;
      background: url('data:image/svg+xml;base64,');
    }
  
    /* --- LAYOUT --- */
    .content-container {
      max-width: 1200px;
      margin: 0 auto;
      position: relative;
      z-index: 2;
      border: var(--grid-line);
      background: var(--bg-manila);
    }
  
    .tech-label {
      font-family: var(--font-tech);
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      opacity: 0.8;
    }
  
    /* --- 1. STAT BLOCK --- */
    .stat-block {
      border-bottom: var(--grid-line);
      position: relative;
    }
  
    /* Hazard Strip Pattern */
    .hazard-strip {
      height: 12px;
      width: 100%;
      background: repeating-linear-gradient(
        45deg,
        var(--ink-black),
        var(--ink-black) 10px,
        var(--bg-manila) 10px,
        var(--bg-manila) 20px
      );
      border-bottom: var(--grid-line);
    }
  
    .stat-content {
      padding: 3rem 2rem;
    }
  
    .meta-row {
      display: flex;
      justify-content: space-between;
      margin-bottom: 1.5rem;
      border-bottom: 1px dashed rgba(17,17,17,0.3);
      padding-bottom: 0.5rem;
    }
  
    .stat-headline {
      font-size: 3rem;
      line-height: 1;
      font-weight: 900;
      text-transform: uppercase;
      margin: 0;
      letter-spacing: -0.02em;
    }
  
    .highlight {
      background: var(--ink-black);
      color: var(--bg-manila);
      padding: 0 0.2em;
    }
  
    /* --- 2. ISSUES GRID --- */
    .issues-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
    }
  
    .issue-card {
      border-right: var(--grid-line);
      padding: 2rem;
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      min-height: 300px;
    }
    
    .issue-card:last-child {
      border-right: none;
    }
  
    .card-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 2rem;
    }
  
    .status-icon {
      width: 24px;
      height: 24px;
      stroke: var(--ink-black);
      stroke-width: 2;
      fill: none;
    }
  
    .issue-card h3 {
      font-size: 1.5rem;
      font-weight: 700;
      text-transform: uppercase;
      margin-bottom: 1rem;
      line-height: 1.1;
    }
  
    .issue-card p {
      font-size: 1rem;
      line-height: 1.5;
      opacity: 0.9;
      margin: 0;
    }
  
    /* --- 3. FOOTER / CHAOS --- */
    .chaos-footer {
      border-top: var(--grid-line);
      display: grid;
      grid-template-columns: 200px 1fr 200px;
      background: rgba(17,17,17, 0.03); /* Subtle tint */
    }
  
    .footer-label, .footer-status {
      padding: 2rem;
      display: flex;
      align-items: center;
      justify-content: center;
    }
  
    .footer-label { border-right: var(--grid-line); }
    .footer-status { border-left: var(--grid-line); }
  
    .footer-content {
      padding: 2rem;
    }
  
    .footer-content h3 {
      font-size: 1.25rem;
      font-weight: 900;
      text-transform: uppercase;
      margin-bottom: 0.5rem;
    }
    
    .footer-content p {
      margin: 0;
      font-family: var(--font-tech);
      font-size: 0.9rem;
    }
  
    .blinking {
      animation: blink 1s step-end infinite;
      background: var(--ink-black);
      color: var(--bg-manila);
      padding: 0.2rem 0.5rem;
    }
  
    @keyframes blink { 50% { opacity: 0; } }
  
    /* --- ANIMATION STATES --- */
    .fade-item {
      opacity: 0;
      transform: translateY(30px);
      transition: opacity 0.6s ease-out, transform 0.6s ease-out;
    }
  
    /* We trigger this class via JS */
    .problem-wrapper.visible .fade-item {
      opacity: 1;
      transform: translateY(0);
    }
    
    /* Stagger delays */
    .problem-wrapper.visible .fade-item:nth-child(1) { transition-delay: 0ms; }
    .problem-wrapper.visible .fade-item:nth-child(2) { transition-delay: 150ms; } /* The Grid container */
    /* If we wanted to stagger grid items individually, we'd target them, but fading the whole row is cleaner */
    .problem-wrapper.visible .fade-item:nth-child(3) { transition-delay: 300ms; }
  
    /* --- RESPONSIVE --- */
    @media (max-width: 900px) {
      .issues-grid {
        grid-template-columns: 1fr;
      }
      .issue-card {
        border-right: none;
        border-bottom: var(--grid-line);
        min-height: auto;
      }
      .stat-headline { font-size: 2rem; }
      
      .chaos-footer {
        grid-template-columns: 1fr;
        grid-template-rows: auto auto auto;
      }
      .footer-label, .footer-status {
        border: none;
        border-bottom: var(--grid-line);
        padding: 1rem;
        justify-content: flex-start;
      }
      .footer-status { border-bottom: none; border-top: var(--grid-line); }
    }
  </style>
  
  <script>
    function initProblemObserver() {
      const section = document.getElementById('problem-trigger');
      
      if (!section) return;
  
      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            section.classList.add('visible');
            // Optional: Unobserve after triggering once
            observer.unobserve(section);
          }
        });
      }, {
        threshold: 0.2 // Trigger when 20% visible
      });
  
      observer.observe(section);
    }
  
    // Run on load and View Transitions
    initProblemObserver();
    document.addEventListener('astro:page-load', initProblemObserver);
  </script>